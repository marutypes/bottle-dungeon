<h1>New Game session</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.game_session_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.game_session_path(@conn, :index) %></span>
