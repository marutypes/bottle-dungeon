<h1>Edit Game session</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.game_session_path(@conn, :update, @game_session)) %>

<span><%= link "Back", to: Routes.game_session_path(@conn, :index) %></span>
